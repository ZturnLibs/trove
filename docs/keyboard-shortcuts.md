# 快捷键一览与设置设计

## 1. 文档目的

定稿 `v1.0` / 当前产品线的快捷键目录、作用域、默认可改范围，以及设置页「快捷键」区块的交互。与[应用菜单](./app-menu-design.md)、[UI 布局与交互](./ui-layout-interaction.md)保持同一套键位语义。

展示用符号：macOS 用 `⌘ ⌥ ⇧ ⌃`；Windows / Linux 用 `Ctrl Alt Shift`。设置存储与代码内部使用 `Command+Shift+Space` 这类可解析字符串。

## 2. 作用域分层

| 层级 | 生效条件 | 用户可否改 | 示例 |
| --- | --- | --- | --- |
| **全局** | 应用在后台也生效（已注册成功时） | 可改；冲突可恢复默认 | 快速记录、统一搜索、剪切板、聚焦主窗 |
| **应用菜单加速键** | 应用为前台时 | `v1.x` 随菜单绑定；高级重映射可后置 | `⌘N` 新建任务、`⌘1`–`⌘5` 导航 |
| **窗口局部** | 焦点在 main / quick 且未落入互斥输入态 | 一般不可改；写入帮助一览 | `Esc` 关闭、`↑↓` 选择 |
| **系统编辑** | 焦点在可编辑控件 | 不改，交给系统 | `⌘C` / `⌘V` |

规则：

1. 同一用户意图只对应**一个**可配置项（例如全局「快速记录」）。
2. 菜单栏显示的加速键必须与配置或本文默认表一致。
3. 输入框内优先系统编辑；`Space` 完成任务等局部键在 `contentEditable` / `input` / `textarea` 中**不触发**。
4. 全局键注册失败时：设置页标记冲突/失败；托盘与菜单仍可用。

## 3. 全局快捷键（可配置）

配置字段：`settings.shortcuts.*`（已实现，设置页可录制改键、保存后立即热重注册、可恢复默认）。

| 配置键 | 意图 | macOS 默认 | Windows 默认 | 动作 |
| --- | --- | --- | --- | --- |
| `quickCapture` | 快速记录 | `⌘⌥Space` | `Ctrl+Alt+Space` | 显示 quick · capture |
| `search` | 统一搜索 / 命令面板 | `⌘⌥F` | `Ctrl+Alt+F` | 显示 quick · search |
| `clipboard` | 剪切板历史 | `⌘⌥C` | `Ctrl+Alt+C` | 显示 quick · clip |
| `focusMain` | 打开主窗口 | `⌘⌥T` | `Ctrl+Alt+T` | 显示并聚焦 main；允许用户关闭该项 |
| `screenshotRegion` | 区域截图 | `⌘⌥X` | `Ctrl+Alt+X` | 截取屏幕区域并入剪切板 |

### 3.1 默认键位选型依据（v2.1 起生效）

默认键位统一采用 `⌘⌥`（macOS）/ `Ctrl+Alt`（Windows）修饰组合，避开以下已知冲突：

| 旧默认 | 已知冲突 | 新默认 |
| --- | --- | --- |
| `⌘⇧Space` / `Ctrl+Shift+Space` | 与「输入法切换 Ctrl+Shift / IME 开关 Ctrl+Space」同修饰链，中文输入下易误触；部分启动器占用 | `⌘⌥Space` / `Ctrl+Alt+Space`（避开 Spotlight `⌘Space`、Raycast/Alfred `⌥Space`、输入源切换 `⌃⌥Space`、表情面板 `⌃⌘Space`） |
| `⌘⇧F` / `Ctrl+Shift+F` | **微软拼音简繁切换 = `Ctrl+Shift+F`**（中文用户高概率冲突）；Office/Outlook 高级查找；VS Code 全局搜索 | `⌘⌥F` / `Ctrl+Alt+F` |
| `⌘⇧V` / `Ctrl+Shift+V` | 主流应用「无格式粘贴」；macOS 剪贴板工具（Paste 等）默认占用 | `⌘⌥C` / `Ctrl+Alt+C`（C = Clipboard） |
| `⌘⇧A` / `Ctrl+Shift+A` | **Chrome / Edge 标签页搜索 = `Ctrl+Shift+A`** | `⌘⌥T` / `Ctrl+Alt+T`（T = Trove；Linux 适配时需复核 GNOME 终端 `Ctrl+Alt+T`） |
| `⌘⇧6` / `Ctrl+Shift+6` | Touch Bar 机型系统截图为 `⌘⇧6`；与系统截图 `⌘⇧3/4/5` 修饰链一致，误触风险高 | `⌘⌥X` / `Ctrl+Alt+X`（避开 QQ 截图 `Ctrl+Alt+A`、微信截图 `Alt+A`、IntelliJ 设置 `Ctrl+Alt+S`） |

已知取舍（记录在案）：

- `Ctrl+Alt` 在 Windows 上等价 AltGr，欧洲键盘布局（波兰、捷克等）下 `Ctrl+Alt+C/X` 可能影响特殊字符输入；产品当前中文用户为主，可接受，用户可自行改键。
- `⌘⌥C` 与部分剪贴板工具（Alfred 剪贴板等）默认键相近；`⌘⌥T` 会覆盖 Safari 标签栏开关（应用内键位，低频）。

### 3.2 旧默认迁移

- 升级时若存储值仍为 v2.0.x 出厂默认（即用户从未自定义），读取设置时**自动迁移**到新默认并持久化（`ShortcutSettings::migrate_legacy_defaults`）。
- 用户自定义过的键位**原样保留**，不受迁移影响。
- 「恢复默认」按钮恢复到本表新默认。

设置交互：

- 列表展示当前绑定；「更改」录制下一组合键（系统保留键会被拒绝）；「恢复默认」调用 `settings_reset_shortcuts`。
- 保存后立即重新注册全局快捷键，无需重启（已实现）。
- 冲突提示文案：「无法注册 {键位}，可能被其他应用占用。可更换快捷键或恢复默认后重试。」

保留键（录制时拒绝绑定为全局，`src/lib/shortcut-record.ts`）：

- 退出 / 关闭语义：`⌘Q`、`⌘W`、`⌘M`、`Alt+F4`、`Ctrl+Q`、`Ctrl+W`、`Ctrl+M`；
- 系统启动器与输入法：`⌘Space`（Spotlight）、`Ctrl+Space`（IME 开关）、`⌃⌘Space`（表情与符号面板）；
- 系统截图：`⌘⇧3`、`⌘⇧4`、`⌘⇧5`；
- 窗口与进程管理：`Alt+Tab`、`Alt+Space`（窗口系统菜单）、`Ctrl+Shift+Esc`（任务管理器）、`⌘⌥Esc`（强制退出）；
- Windows / Linux 上仅含 Win（Super）键的组合一律拒绝（避免劫持 `Win+E`、`Win+D` 等系统键）。

## 4. 应用内导航与创建（菜单加速键）

| 意图 | macOS | Windows | 菜单 ID / 说明 |
| --- | --- | --- | --- |
| 今日 | `⌘1` | `Ctrl+1` | `menu.view.today` |
| 收件箱 | `⌘2` | `Ctrl+2` | `menu.view.inbox` |
| 任务 | `⌘3` | `Ctrl+3` | `menu.view.tasks` |
| 记忆 | `⌘4` | `Ctrl+4` | `menu.view.memory` |
| 剪切板页 | `⌘5` | `Ctrl+5` | `menu.view.clipboard` |
| 新建任务 | `⌘N` | `Ctrl+N` | `menu.file.new_task` |
| 新建提醒 | `⌘⇧N` | `Ctrl+Shift+N` | `menu.file.new_reminder` |
| 新建记忆 | `⌥⌘N` | `Ctrl+Alt+N` | `menu.file.new_memory` |
| 设置 | `⌘,` | `Ctrl+,` | `menu.app.settings` / `menu.file.settings` |
| 关闭窗口（隐藏） | `⌘W` | `Ctrl+W` | `menu.file.close_window` |
| 退出应用 | `⌘Q` | `Ctrl+Q` | `menu.app.quit` / `menu.file.quit` |
| 查找（顶栏） | `⌘F` | `Ctrl+F` | `menu.edit.find` |
| 命令面板式搜索（主窗内） | `⌘K` | `Ctrl+K` | 聚焦顶栏或打开与全局 search 同源面板；与全局 `⌘⌥F` 并存 |
| 切换详情面板 | `⌘⇧D` | `Ctrl+Shift+D` | `menu.view.detail` |

`⌘N` 语义：始终「新建任务」，不随当前页变成新建记忆（新建记忆用独立键）。页面「+」按钮可按模块默认类型，与菜单可不同。

## 5. 当前选中实体（项目菜单 / 列表）

| 意图 | macOS | Windows | 条件 |
| --- | --- | --- | --- |
| 完成 / 取消完成任务 | `Space` | `Space` | 列表焦点，非输入框 |
| 完成（菜单） | `⌘.` | `Ctrl+.` | 任务选中 |
| 删除… | `⌘⌫` | `Ctrl+Backspace` | 需确认 |
| 再次复制剪切板项 | `⌘⇧C` | `Ctrl+Shift+C` | 可选；无则仅用 Enter（quick） |

多选：`⇧` 范围选、`⌘/Ctrl` 点选；批量完成仍可用 `Space`。

## 6. 主窗口列表

| 意图 | 键 | 说明 |
| --- | --- | --- |
| 上一项 / 下一项 | `↑` `↓` | 必选 |
| 上一项 / 下一项（可选） | `k` `j` | 非输入焦点；设置中可关 |
| 打开 / 聚焦详情编辑 | `Enter` | |
| 退出编辑回列表 | `Esc` | 详情内 |
| 主操作（完成/收藏等） | `⌘Enter` | 详情内；与类型相关 |

### 6.1 今日页 `/today`（列表焦点，非输入框）

| 意图 | 键 | 说明 |
| --- | --- | --- |
| 加入 / 移出今日重点 | `F` | 选中任务时切换；已加入则移出 |
| 移出今日重点 | `⇧F` | 仅当任务已在今日重点时生效 |
| 进入专注模式 | `Enter` | 选中任务已在今日重点且为待办时 |
| 进入专注模式 | `⌘Enter` | 选中任意待办任务时（今日页） |
| 推迟显示 | `D` | 待接：打开 DeferPicker（详情页已可用） |
| 标记 / 编辑等待 | `W` | 待接：打开等待表单（详情页已可用） |
| 完成 / 取消完成 | `Space` | 与 §5 相同；输入框内不触发 |
| 撤销最近操作 | `⌘Z` | recent-actions 栈；与 toast 撤销一致 |

拖拽：今日任务拖入「今日重点」区 = 加入重点；从重点区拖回 = 移出（不改变截止日期）。

入口（无专属快捷键）：顶栏「专注」（选中项在今日重点时）、详情「开始专注」、任务页详情「开始专注」。

### 6.2 专注层（全屏 overlay，非输入框）

| 意图 | 键 | 说明 |
| --- | --- | --- |
| 退出确认 | `Esc` | 弹出「保持待办并退出 / 继续专注」 |
| 完成任务 | — | 点击「完成任务」按钮（`⌘Enter` 待增强） |
| 保持待办并退出 | — | 点击底部按钮或 Esc 确认 |

专注层打开时主窗口列表快捷键不生效。倒计时到点触发系统通知（需通知权限）；「稍后 5 分钟」仅延长本地计时，不改任务。

### 6.3 每日收尾向导（Modal）

| 意图 | 键 | 说明 |
| --- | --- | --- |
| 退出向导 | `Esc` | 未完成 run 保持进行中，可再次打开 |
| 跳过 / 下一步 | — | 底部按钮；Step 1–3 可跳过，摘要步不可跳过 |

Step 1 每项决策立即生效并进入 recent-actions 撤销栈；**无**「全部延期」类批量操作。

## 7. 快捷窗口 `quick`

### 7.1 各 mode 共用

| 意图 | 键 |
| --- | --- |
| 关闭 | `Esc`（有输入时可先清空再关，见布局文档） |
| 切换 mode | `⌘1` / `⌘2` / `⌘3` 仅在 quick 内映射为 记录/搜索/剪切板；**不**与主窗导航冲突因焦点在 quick |

若实现成本高：用 `Tab` 在分段控件间移动，或 `⌘⇧[` / `⌘⇧]` 循环 mode。

### 7.2 Capture

| 意图 | 键 |
| --- | --- |
| 提交创建 | `⌘Enter` / `Ctrl+Enter` |
| 类型切换 | `⌘⇧↑/↓` 或控件内左右；展示「任务｜提醒｜记忆」 |

### 7.3 Search（含命令面板）

| 意图 | 键 |
| --- | --- |
| 移动选中 | `↑` `↓` |
| 执行命令 / 打开结果 | `Enter` |
| 完成任务（结果为任务时） | `⌘Enter` / `Ctrl+Enter` |
| 延期（结果为任务时） | `⌘⇧Enter` / `Ctrl+Shift+Enter` |

底栏固定提示，与当前实现一致。

### 7.4 Clip

| 意图 | 键 |
| --- | --- |
| 再次复制并隐藏 | `Enter` |
| 尝试直接粘贴 | `⌘Enter` / `Ctrl+Enter` |
| 移动选中 | `↑` `↓` |

无辅助功能权限时：`⌘Enter` 降级为再次复制，或禁用并 toast「已复制，可手动粘贴」。

## 8. 帮助一览与设置页

### 8.1 「快捷键一览」对话框 / 设置锚点

分组展示，只读 +「在设置中更改」链到可改项：

1. 全局  
2. 导航与创建  
3. 列表与项目  
4. 快捷窗口  

每行：意图 | 当前键位 | 作用域标签（全局 / 应用内）。

菜单「帮助 → 快捷键一览」打开此视图。

### 8.2 设置页文案

标题：快捷键  

说明：全局快捷键在应用启动时注册，即使主窗口关闭也可唤起。若与其他应用冲突，请更换键位或恢复默认后重试。

| UI 元素 | 文案 |
| --- | --- |
| 快速记录 | 在任意应用上快速创建任务、提醒或记忆 |
| 统一搜索 | 搜索内容并执行常用命令 |
| 剪切板历史 | 打开剪切板浮层 |
| 打开主窗口 | 显示完整工作台（可关闭此全局键） |
| 恢复默认 | 按钮 |
| 重启提示 | 仅当热更新注册失败时：「已保存，请重启应用使全局快捷键生效」 |

## 9. 实现状态对照

| 能力 | 状态（以仓库为准） |
| --- | --- |
| 四项全局默认注册 | 已有 |
| 设置中展示与恢复默认 | 已有 |
| 热切换重新注册 / 录制改键 | 已有（保存后立即重注册；系统保留键拒绝录制；v2.1 起新默认 + 旧默认自动迁移） |
| 菜单栏加速键完整集 | 待按[应用菜单](./app-menu-design.md)补齐 |
| 主窗 `⌘1`–`⌘5` | 待接 |
| quick 内搜索命令面板键位 | 已有 Enter / ⌘Enter / ⌘⇧Enter |
| 今日页 `F` / `⇧F` 今日重点 | 已有 |
| 今日页 `Enter` / `⌘Enter` 进入专注 | 已有 |
| 专注层 `Esc` 退出确认 | 已有 |
| 每日收尾向导 `Esc` 退出 | 已有 |
| 每周回顾页 | — | 无专属快捷键；Today / 设置入口 |
| 健康仪表盘 | — | 无专属快捷键；设置入口 |
| 今日页 `D` / `W` 推迟与等待 | 详情页可用；列表快捷键待接 |

## 10. 验收标准

1. 四项全局默认在 macOS / Windows 上可唤起对应窗口；冲突时有可见提示。  
2. 帮助或设置中的一览与本文默认表一致。  
3. 输入框内 `Space` / `j` `k` 不触发列表动作。  
4. `⌘W` 隐藏窗口，`⌘Q` 退出进程。  
5. quick search 底栏提示与真实键位一致。  
6. 「恢复默认」后配置与 §3 表一致。
