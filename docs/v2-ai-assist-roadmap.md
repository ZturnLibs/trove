# v2.0 路线图：可控智能辅助

> 状态：**已全部交付**（8/8 切片，2026-08）
> 设计合同：[post-v1-iteration-design.md](./post-v1-iteration-design.md) §9
> 启动评估：`.trellis/tasks/08-19-v2-0-ai-assist-roadmap/research/startup-gate-assessment.md`

## 1. 定位

AI 是既有任务、记忆、搜索流程中的辅助能力：**建议必须带来源、修改必须经确认、功能可逐项关闭**。不建聊天主页，不做无确认写操作，评分/评价类功能一律不做。

## 2. 启动门槛结论（§9.1）

| 门槛 | 结论 |
| --- | --- |
| v1.x 数据模型与动作层稳定 | ✅ v1.4.0 WorkbenchAction 三层共用（trove:// / trove-cli / 自动化） |
| 观察到规则/模板无法解决的高频问题 | ✅ NL 解析仅确定性关键词、FTS5 无语义匹配、长文本提取无解（代码级证据见评估文档） |
| 本地/远程数据边界清晰 | ✅ 本路线图冻结（见 §4） |
| AI 失败/关闭基础可用 | ✅ 架构约束，切片 1 验收兑现 |

## 3. 切片地图

| Wave | 切片 | 内容 |
| --- | --- | --- |
| A | AI 服务边界 ✅ | provider 抽象（off/本地 Ollama/自定义远程）、AISuggestion + 来源引用、设置页授权与审计、评估样本 |
| A | 复杂信息提取 ✅ | 长文本 → 任务草稿列表，原文引用、逐项确认、模糊日期待确认 |
| B | 相关内容建议 ✅ | 任务推荐相关记忆/剪贴板，确认后写 EntityLink，拒绝降频 |
| B | 周期回顾摘要 ✅ | weekly_review 确定性统计 + 模型组织中文摘要 + 数字徽标对照 |
| B | 每日工作建议 ✅ | 今日重点候选 + 特征引用理由，加入走既有撤销链路 |
| C | 检查项数据模型 ✅ | 任务 checklist 模型与 UI（进度徽标、冻结、级联） |
| C | 任务拆分 ✅ | 任务说明 → 候选检查项（依据子串防编造），勾选写入 |
| C | 语义检索 ✅ | 可重建向量索引（2 万行上限/排除范围），关键词/语义双列 |

## 4. 数据边界（冻结）

- **永远本地**：分类规则、确定性统计、建议合并排序、确认门禁、审计记录。
- **发送前过滤**：密码管理器来源剪贴板、标记敏感记忆、排除应用内容（`sanitize_context`）。
- **仅当用户选远程 provider 才发送**：任务/记忆标题与正文片段、OCR 文本（最小必要上下文）。
- 远程 key 存本机，不进备份导出与日志；派生数据（建议历史、向量索引）不纳入主备份、可一键清空。

## 5. 验收标准

继承 §9.6 全部条款，核心：

- AI 关闭/不可用 → 全部 v1.x 功能正常（Wave A 结束时实测复核）。
- 每条修改性建议经用户确认；提取/摘要可跳转原始来源。
- 统计数字由查询产生，模型只组织文字；错误建议可拒绝且不改动原文。
- `fixtures/ai-eval/` 固定样本回归（日期提取、任务提取、搜索相关性）。
